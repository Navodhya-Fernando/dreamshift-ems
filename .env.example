# DreamShift EMS - Environment Variables Configuration

# ==========================================
# REQUIRED: MongoDB Database Configuration
# ==========================================

# MongoDB Connection URI
# For MongoDB Atlas (Cloud - Free tier available):
#   1. Create account at mongodb.com/cloud/atlas
#   2. Create free cluster
#   3. Get connection string from "Connect" → "Connect your application"
#   4. Replace <username>, <password>, and <cluster-url>
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/?retryWrites=true&w=majority

# For Local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/

# Database Name (can be any name you want)
DB_NAME=dreamshift_ems


# ==========================================
# OPTIONAL: Google Calendar Integration
# ==========================================

# Google OAuth Credentials
# To enable Google Calendar sync:
#   1. Go to console.cloud.google.com
#   2. Create new project
#   3. Enable Google Calendar API
#   4. Create OAuth 2.0 credentials
#   5. Download JSON and extract client ID & secret
# GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your_client_secret


# ==========================================
# OPTIONAL: Application Settings
# ==========================================

# Secret key for sessions (auto-generated if not set)
# SECRET_KEY=your_random_secret_key_here

# Debug mode (set to False in production)
# DEBUG=True


# ==========================================
# SETUP INSTRUCTIONS
# ==========================================

# 1. Copy this file and rename to .env (remove .example)
#    cp .env.example .env

# 2. Fill in MONGODB_URI and DB_NAME (required)

# 3. (Optional) Add email and calendar credentials

# 4. Make sure .env is in .gitignore (security!)

# 5. Run the app:
#    streamlit run app.py

# ==========================================
# SECURITY NOTE
# ==========================================
# NEVER commit .env file to git!
# .env contains sensitive credentials
# Always use .env.example for sharing configuration template
